package com.ticmas.patricio.proyectofinal;
/*

import androidx.test.espresso.Espresso.*;
import androidx.test.espresso.matcher.ViewMatchers.*;
import androidx.test.ext.junit.rules.ActivityScenarioRule;

import com.ticmas.patricio.proyectofinal.view.MainActivity;

import org.junit.Rule;
import org.junit.Test;

class TestUI {

    @get:Rule
    val activityRule = ActivityScenarioRule(MainActivity::class.java)

    @Test
    fun testCompareAndShowResult() {
        val firstString = "Hello"
        val secondString = "Hello"
        val expectedResult = "Las cadenas son iguales"

        onView(withId(R.id.etInput)).perform(typeText(firstString))
        onView(withId(R.id.etInput2)).perform(typeText(secondString))
        closeSoftKeyboard()
        onView(withId(R.id.btnComparar)).perform(click())

        onView(withId(R.id.Resultado)).check(matches(withText(expectedResult)))
    }
}*/
